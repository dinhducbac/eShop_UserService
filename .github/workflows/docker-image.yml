name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
     - name: Check out code
       uses: actions/checkout@v3
        
     - name: Build frontend image
       run: cd D:\\.Net_Core_Exercises\\eShopService\\eShop_UserService\\Exercise2
     - name: Build frontend image 1
       run: docker image build -t image .
